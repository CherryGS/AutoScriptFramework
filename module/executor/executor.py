"""
原子性(全部执行/全部不执行) , 可见性(对公共变量的修改是否会立即反应) , 有序性(代码重排)
"""
import asyncio
from typing import Iterable


class BaseExecutor:
    """
    NotImplement
    """


class AsyncExecutor:
    """
    NotImplement
    """
